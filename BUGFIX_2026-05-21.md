# Bug 修复记录 — 2026-05-21 ~ 2026-05-22

## 问题 1: Timeout 错误 — 云函数调用无限挂起

**现象**: 点击「开始记账」按钮后，控制台输出:
```
Error: timeout
    at Function.<anonymous> (WAServiceMainContext.js?t=wechat&v=3.15.2:1)
```

**根因**（三层叠加）:

1. **`cloud.ts` 未接入真实云调用** — `callCloudFunction` 只有 mock 实现，没有真实调用 `wx.cloud.callFunction`
2. **dist `project.config.json` 缺失 `cloudfunctionRoot`** — uni-app 构建时重新生成该文件，不会继承源文件中的云函数目录配置
3. **云开发环境未开通** — 游客 `touristappid` 无云环境，`wx.cloud.callFunction` 在原生层无限等待。配置真实 AppID 后，仍需手动开通云环境并关联到项目

**排查过程**:

添加关键节点日志，确认 SDK 初始化正常，问题在云端无响应:

```
✅ wx 存在: true
✅ wx.cloud 存在: true
✅ wx.cloud.init 类型: function
✅ wx.cloud.init 调用成功
❌ 云函数 login 调用超时 (8000ms) ← 云环境无响应
```

**修复**:

| 层面 | 修复内容 |
|---|---|
| `cloud.ts` | 重写为真实 `wx.cloud.callFunction`，mock 保留给测试，超时 8s→30s（冷启动容错） |
| `App.vue` | 移除 `onLaunch` 中的 `initCloud()`，改为 `callCloudFunction` 内懒初始化 |
| `manifest.json` | 真实 AppID `wxb15dc0bf97a52b63`，`cloud: true` |
| `vite.config.ts` | `closeBundle` 自动补写 `cloudfunctionRoot`，兼容 dev/build 双路径 |
| 云环境 | 指定环境 ID `cloud1-d2goyji7jb6c9f8b5` |
| 数据库 | 创建 `users`、`bills`、`memos` 三个集合 |

---

## 问题 2: dist 输出缺失 cloudfunctionRoot

**现象**: `npm run dev:mp` 后，`dist/dev/mp-weixin/project.config.json` 没有 `cloudfunctionRoot`。

**根因**: uni-app 构建时重新生成 `project.config.json`，不继承源文件中的自定义字段。

**修复**: `vite.config.ts` 自定义插件添加 `buildStart`（复制云函数目录）+ `closeBundle`（补写 `cloudfunctionRoot`），轮询 dev/build 双路径。

---

## 问题 3: 云函数缺少 package.json

**现象**: 6 个云函数（`clearBills`、`getProfileSummary`、`getStatistics`、`listBillsByMonth`、`login`、`saveUserProfile`）只有 `index.js`，没有 `package.json`。微信新版云环境不再预装 `wx-server-sdk`，导致这些云函数部署后无法运行。

**修复**:
- 补全 6 个缺失的 `package.json`（`wx-server-sdk: latest`）
- 新增 `postinstall` 脚本到 `package.json`，`npm install` 时自动为 14 个云函数安装依赖
- `.gitignore` 新增 `cloudfunctions/*/package-lock.json`

---

## 问题 4: chooseAvatar 模拟器报错（Windows）

**现象**: 点击头像选择按钮时:
```
chooseAvatar:fail Error: ENOENT: no such file or directory,
open '...\WeappSimulator\...\tmp\K_PWfz4DK4h...jpeg'
```

**根因**: 微信开发者工具 Windows 版模拟器 bug，`chooseAvatar` 生成的临时文件路径在模拟器中无法访问。真机无此问题。

**修复**: `<img>` 标签添加 `@error="tempAvatarUrl = ''"`，加载失败时回退到默认头像占位。

---

## 问题 5: window.innerWidth 在小程序环境报错

**现象**:
```
TypeError: Cannot read property 'innerWidth' of undefined
    at setup (index.js)
```

**根因**: `FloatingBillButton` 组件中直接使用了 `window.innerWidth` / `window.innerHeight`，微信小程序没有 `window` 对象。

**修复**: 封装 `screenWidth()` / `screenHeight()` 函数，优先使用 `uni.getSystemInfoSync()`，web 环境回退 `window.innerWidth`。

---

## 问题 6: FloatingBillButton 多处小程序兼容问题

**现象 6a**: 拖拽时报 `TypeError: Cannot read property 'addEventListener' of undefined`
- **根因**: 微信小程序没有 `document` 对象
- **修复**: `document.addEventListener/removeEventListener` 加 `typeof document !== 'undefined'` 守卫；touch 事件通过模板 `@touchmove`/`@touchend` 绑定
- **提交**: `src/components/FloatingBillButton/index.vue`

**现象 6b**: `screenWidth()` / `screenHeight()` 死循环导致堆栈溢出
- **根因**: 回退分支 `return screenWidth()` 递归调用自身，没有兜底
- **修复**: 回退默认值 375/667

**现象 6c**: `wx.getSystemInfoSync` 弃用警告
- **根因**: 微信新版 API 不再推荐 `getSystemInfoSync`
- **修复**: 替换为 `uni.getWindowInfo().windowWidth/Height`

**现象 6d**: 点击 + 按钮无响应，无法跳转记账页
- **根因**: `@touchstart.prevent` 阻止了 click 事件合成 + `onClick` 依赖原生 click 事件；且 `onDragMove` 没有移动阈值，手指微抖即标记为拖动
- **修复**: 去掉 `@click`；点击逻辑移至 `onDragEnd`，`hasMoved` 为 false 时 `emit('click')`；增加 `MOVE_THRESHOLD = 5px`；去掉 H5 独有的 mouse 事件

---

## 问题 7: 记账页功能缺陷

**现象 7a**: 计算器不自动显示结果（如输入 "100+50" 仍显示 "50" 而非 "150"）
- **根因**: `displayFormatted` 只显示 `displayValue`，没有计算运行中结果
- **修复**: 新增 `runningResult` computed，有 pendingOperator 时实时计算 `accumulator ± current`，显示即时运算结果

**现象 7b**: 日期选择器不可用
- **根因**: 使用 HTML `<input type="date">`，微信小程序不支持
- **修复**: 替换为 `<picker mode="date" fields="month">`（微信原生日期选择器），`onDateChange` 兼容小程序 `e.detail.value` 和 H5 `e.target.value`

**现象 7c**: 收入/支出切换按钮样式简陋且有外边框
- **根因**: 普通 `<button>` 标签，微信小程序默认有 button `::after` 伪元素边框
- **修复**: 重新设计为药丸式滑块（pill toggle）；全面重置 button：`border: none; outline: none; -webkit-appearance: none; box-shadow: none;` + `::after { content: none; border: none; }`

**现象 7d**: 计算精度问题（浮点数 0.1+0.2≠0.3）
- **修复**: 引入 `decimal.js`，所有计算改用 `Decimal.plus()` / `Decimal.minus()`

**现象 7e**: 输入小数点 "." 不显示
- **根因**: `displayFormatted` 用 `parseFloat(displayValue)` 转换为数字，"123." → 123，丢失尾点
- **修复**: 无 pendingOperator 时直接显示原始 `displayValue`（`¥ ${displayValue.value}`）

**现象 7f**: 输入过程自动补零（如 "123" 显示 "¥ 123.00"）
- **根因**: `toFixed(2)` 强制两位小数
- **修复**: 无操作符时直接显示原始输入；有操作符时计算结果用 `.replace(/\.?0+$/, '')` 去掉多余尾零

---

## 问题 8: 明细页功能不可用

**现象 8a**: 日期筛选（MonthPicker）点击无响应
- **根因**: 使用 HTML `<select>` + `<option>`，微信小程序不支持
- **修复**: 重写为 `<picker mode="date" fields="month">`，`onChange` 提取 `YYYY-MM`

**现象 8b**: 点击账单列表项无法进入编辑页
- **根因**: `goToBillEdit` 函数体只有注释，没有实际跳转
- **修复**: `uni.navigateTo({ url: '/pages/bill-edit/index?id=${billId}' })`

---

## 新增功能

| 功能 | 文件 | 说明 |
|------|------|------|
| 左滑删除 | `src/components/SwipeAction/index.vue` | Touch 事件驱动，滑动 >40px 吸附展开删除按钮，带确认弹窗 |
| 底部导航 | `src/components/BottomNav/index.vue` | 明细/统计/我的 三 tab，`uni.reLaunch` 切换，使用 Unicode 符号 ☰ ▤ ◉ |
| 底部导航集成 | `detail/index.vue`、`statistics/index.vue`、`profile/index.vue` | 三页面底部统一 padding 避让导航栏 |

---

## 问题 9: getStatistics 云函数超时

**现象**: 统计页面加载超时（>30s），`callFunctionWithData` 抛出 "云函数调用超时"

**根因**（三层）:
1. **2 次独立全量查询** — 原函数分别查询当月账单和最近 7 天账单，数据集大时重复扫描
2. **无分页** — `.get()` 默认最多返回 100 条，数据超 100 条时漏数据，但 `count()` 扫描全量造成 timeout
3. **无日期索引** — `billDate` 无索引，每次按 `openid` 查到后全量内存过滤

**修复**:

| 优化 | 说明 |
|------|------|
| 2次→1次查询 | 合并为 `_.in(allDates)` 一次查完，内存分流为 monthBills / dailyBills |
| 循环分页 | `getAllBills()` 用 `skip+limit` 逐批拉取，去掉冗余 `count()` 调用 |
| 精确日期范围 | 只扫目标月份 + 最近 7 天的 38 个日期，不走全表 |

**注意**: 已在云开发控制台创建复合索引 `(openid, billDate)`，建议保持，排序方向走内存排序即可。

---

## 涉及文件总览

| 文件 | 修复内容 |
|---|---|
| `src/services/cloud.ts` | 真实 `wx.cloud.callFunction` + 30s 超时 + 指定云环境 ID |
| `src/App.vue` | 移除 eager `initCloud()`，改为 lazy init |
| `src/manifest.json` | 真实 AppID |
| `vite.config.ts` | 自动补写 `cloudfunctionRoot` + 复制云函数 |
| `src/pages/auth/index.vue` | 头像加载失败回退兜底 |
| `src/components/FloatingBillButton/index.vue` | `window.innerWidth` → `uni.getSystemInfoSync()`，document 守卫，触摸事件重写，点击逻辑修复 |
| `src/pages/bill-edit/index.vue` | 计算器实时运算、decimal.js 精度、picker 日期选择、pill toggle 样式、按钮边框 reset、显示逻辑重写 |
| `src/pages/detail/index.vue` | `goToBillEdit` 导航、SwipeAction 左滑删除、BottomNav 集成 |
| `src/pages/statistics/index.vue` | BottomNav 集成、padding 避让 |
| `src/pages/profile/index.vue` | BottomNav 集成 |
| `src/components/MonthPicker/index.vue` | `<select>` → `<picker mode="date" fields="month">` |
| `src/components/SwipeAction/index.vue` | 新增：左滑删除组件 |
| `src/components/BottomNav/index.vue` | 新增：底部导航组件 |
| `cloudfunctions/getStatistics/index.js` | 单次查询 + 循环分页 + 精确日期范围 |
| `cloudfunctions/*/package.json` | 补全 6 个缺失的依赖声明 |
| `package.json` | 新增 `postinstall` 脚本 |
| `.gitignore` | 优化云函数依赖忽略规则 |

---

## 小结

| # | 问题 | 状态 |
|---|---|---|
| 1 | 云函数调用超时 | ✅ 已修复 |
| 2 | dist 缺少 cloudfunctionRoot | ✅ 已修复 |
| 3 | 云函数缺少 package.json | ✅ 已修复 |
| 4 | chooseAvatar 模拟器报错 | ✅ 已修复（真机无此问题） |
| 5 | window.innerWidth 小程序报错 | ✅ 已修复 |
| 6 | FloatingBillButton 兼容性 (document/点击/deprecated) | ✅ 已修复 |
| 7 | 记账页功能 (计算器/日期/精度/样式/显示) | ✅ 已修复 |
| 8 | 明细页功能 (日期筛选/列表跳转) | ✅ 已修复 |
| — | 左滑删除 + 底部导航（新增功能） | ✅ 已完成 |
| 9 | getStatistics 云函数超时 | ✅ 已修复 |
