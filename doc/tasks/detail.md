# 明细模块任务清单

参考：`doc/proposal.md`（第 8、15.1、15.2 节），`doc/high-level-design.md`（第 4、5、6、9、11 节）

## 最小可执行任务（Vibe Coding）

- [ ] 创建明细页骨架：`/pages/detail/index`，包含顶部汇总区、日期分组列表、空状态区。
- [ ] 按 `doc/ui.md` 8.2 实现明细页 UI：顶部卡片、金额颜色语义、日期分组样式、列表排版与对齐。
- [ ] 实现年月选择状态与切换交互，默认当前年月。
- [ ] 实现 `listBillsByMonth` 云函数：返回指定年月账单列表和月收入/月支出汇总。
- [ ] 在前端实现账单按 `billDate` 分组展示与日期标题格式化。
- [ ] 实现列表项展示规则：分类、备注（可空）、金额正负号。
- [ ] 实现空状态展示：当前月无账单时展示引导文案。
- [ ] 接入明细页加载态：首次骨架屏、列表加载提示、无数据空状态。
- [ ] 实现账单项点击进入编辑页，并带上账单 ID。
- [ ] 接入全局刷新标记：账单变更后自动刷新当前年月明细。
- [ ] 自测：切换月份、空月、跨月新增后回跳定位、编辑后刷新。
- [ ] 自测：明细页 UI 断言（金额颜色、日期标题格式、空状态渲染）。
